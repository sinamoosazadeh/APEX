# Research platform configuration - owned by Phase 11 (Book II ch. 14/23).
schema_version: 1

learning:                        # AICE online-learning inputs (Book VI gLearn/gCal)
  feature_prior: 6.0
  adapt_strength: 0.65
  feature_minimum_sample: 25.0
  calibration_minimum_sample: 25.0
  calibration_maximum_blend: 0.65

orchestrator:
  default_window_bars: 480       # job window when a job carries none

promotion:                       # shadow deployment (Book II 14.24/22.19)
  shadow_min_bars: 96            # forward bars required before a verdict
  shadow_horizon_bars: 48        # simulated trade horizon inside the shadow fold
  shadow_tolerance: 0.05         # allowed score shortfall vs the incumbent

experiments: {}
